#include "holberton.h"
#include <stdio.h>
/**
 * _abs - main check the code for Holberton School students.
 * Return: (0)
 * @a: I need this parameter for this function
 */
int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (-a);
	}
}
